/*---- metals_help_text ----*/
 
switch (FEAT_INDUSTRIES, SELF, metals_extra_text, [
    STORE_TEMP(100 - max(efficiency_waste, processing_total_production_efficiency_malus()), 257),   // returns production efficiency
]) {return string(STR_METALS_EXTRA_TEXT);}

