/*---- components_help_text ----*/
 
switch (FEAT_INDUSTRIES, SELF, components_extra_text, [
    STORE_TEMP(100 - manufacturing_total_production_efficiency_malus(), 257),   // returns production run efficiency
	STORE_TEMP(manufacturing_total_production(), 258)   // returns the total number of production runs active
]) {return string(STR_COMPONENTS_EXTRA_TEXT);}