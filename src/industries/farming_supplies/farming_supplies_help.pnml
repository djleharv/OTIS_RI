/*---- farming_supplies_help_text ----*/
 
switch (FEAT_INDUSTRIES, SELF, farming_supplies_extra_text, [
    STORE_TEMP(((total_production - manufacturing_synergy_production_efficiency() - total_production_efficiency()) * 100) / total_production , 257),   // returns production run efficiency
	STORE_TEMP(manufacturing_total_production_runs(), 258)   // returns the total number of production runs active
]) {return string(STR_FARMING_SUPPLIES_EXTRA_TEXT);}