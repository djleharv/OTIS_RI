/*---- refinement_help_text ----*/
 
switch (FEAT_INDUSTRIES, SELF, refinement_extra_text, [
    STORE_TEMP(100 - (total_efficiency_malus - processing_total_production_efficiency_malus()), 257),   // returns production efficiency
	STORE_TEMP(processing_total_production(), 258)   // returns the current production level
]) {return string(STR_REFINEMENT_EXTRA_TEXT);}

