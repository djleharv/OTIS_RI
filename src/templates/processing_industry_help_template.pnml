/*---- _industry_item_name__help_text ----*/
 
switch (FEAT_INDUSTRIES, SELF, _industry_item_name__extra_text, [
    STORE_TEMP(((total_production - _industry_type__synergy_production_efficiency() - total_production_efficiency()) * 100) / total_production , 257),   // returns production run efficiency
	STORE_TEMP(_industry_type__total_production_runs(), 258)   // returns the total number of production runs active
]) {return string(STR__string__EXTRA_TEXT);}